#include <windows.h>

int APIENTRY WinMain(_In_ HINSTANCE histance,
                     _In_opt_ HISTANCE hPrevInstance,
                     _In_ LPSTR lpCmdLine,
                     _in_ int nCmdShow){

    MessageBox(NULL, "Oi Windows!", "Mensagem", 0);

    return 0;
}
